{
  "name": "hotcakey",
  "version": "0.1.0",
  "description": "the cross platform hot-key tool for electron and node.js",
  "main": "index.js",
  "scripts": {
    "cc": "bear -- npm run build",
    "build": "node-gyp configure && node-gyp build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "demo": "node src/index.js"
  },
  "keywords": [
    "hotkey",
    "shortcut"
  ],
  "author": "daylilyfield",
  "license": "MIT",
  "devDependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^3.2.1",
    "node-gyp": "^8.1.0"
  },
  "dependencies": {}
}
